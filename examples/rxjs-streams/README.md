# rxjs-streams

Pipes a topic through `bufferTime` + `filter` + `map` to turn a stream of page views into windowed summaries. Demonstrates how seamlessly RxJS layers on top of any adapter.

```sh
pnpm start
```

No external services required.
